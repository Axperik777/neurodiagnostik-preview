const signature=document.querySelector('.energy-signature');if(signature){signature.addEventListener('pointermove',event=>{const box=signature.getBoundingClientRect();signature.style.setProperty('--mx',`${event.clientX-box.left}px`);signature.style.setProperty('--my',`${event.clientY-box.top}px`)})}const observed=document.querySelectorAll('.signature-copy,.signature-orbit,.card,.route>*');observed.forEach(element=>element.classList.add('reveal'));if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.14});observed.forEach(element=>observer.observe(element))}else{observed.forEach(element=>element.classList.add('visible'))}
